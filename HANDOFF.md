# GuildLog handoff (2026-09-19): next up is a site security audit

Repo: `C:\Users\blewm\OneDrive\Desktop\Claude Code`, branch `main`, remote `blewmackee-hub/guildlog-loot-tracker`.
Read `CLAUDE.md` first (commands, architecture, gotchas). `AGENTS.md` is auto-generated by `next dev`; don't edit it.

## Next task: security audit
Goal: audit the deployed site's code for vulnerabilities (the user has not narrowed it further; ask what they want if unclear).

Stack facts that matter for it:
- Next.js 16 App Router, next-auth v5 beta (Discord OAuth only, `src/auth.js`), Postgres via `pg` (`src/lib/db.js`, schema `db/schema.sql`), `bcryptjs` used in `src/lib/guilds.js` (guild PINs, `checkPinRateLimit`).
- Attack surface: 20+ API routes in `src/app/api/` (admin, auth, character, guild/*, guilds/*), session helpers `src/lib/apiHelpers.js` (`requireDiscordId`, `requireActiveGuild`, `requireAdmin`), `src/lib/guildSession.js` (active-character cookie), `src/lib/admin.js` (`ADMIN_DISCORD_IDS` env allowlist), `src/lib/guilds.js` (721 lines, all DB logic).
- Things worth checking first: authorization on every route (guild membership/officer/owner checks, IDOR on ids in URLs like `guilds/[id]/join`, `guild/parties/[id]`, `admin/guilds/[id]`), SQL parameterization in `guilds.js`, PIN rate limiting and hashing, the active-character cookie (signed? httpOnly? tamperable?), CSRF/origin handling on mutating routes, `AUTH_SECRET`/env handling, admin route gating, unvalidated JSON bodies, and the `/api/character` PUT that stores the whole `build`/`wishlist` JSON.
- No test suite exists.

Claude Security plugin (`/claude-security:claude-security`) attempt so far:
- Needs the `Workflow` tool: it was deferred, load it with `ToolSearch` query `select:Workflow` before the scan job (the first try failed for that reason).
- The user picked a scoped scan (API + auth layer: `src/app/api`, `src/auth.js`, `src/lib`, about 30 files) at `low` effort because of plan limits, confirmed "Yes", then interrupted to write a custom prompt. Nothing was created or run. The recipe requires the fixed cost-confirmation question again before starting.
- Plan limits at the time (Pro): 5-hour window 14% used (resets 14:10 UTC), weekly 35%. Extra usage is off. A whole-repo medium scan would likely burn a large share of a window; keep scope small or effort low. Check `mcp__ccd_session_mgmt__get_usage` first.

## State of the repo
Committed and pushed this session (latest `a343eaa`):
- Sep 2026 patch data: 26 new S1 armor/accessory items, bosses Grimturg and Exodus, item level caps 80 to 90 and Ramux archboss weapons 85 to 93 (`src/data/loot-data.json`), new icons served from questlog.gg while the CDN lacks them.
- `scripts/add-questlog-items.js` plus the `questlog-scrape` skill (`.claude/skills/questlog-scrape/SKILL.md`) for future patches.
- Party Planner: compact one-line centered dropdowns; Inheritance ore-cost formula refit (`0.155*L - 3.27` per level, extrapolated above 81).
- Speed work: Database list windowed to 60 rows with an IntersectionObserver tripwire; page background moved from `body{background-attachment:fixed}` to `body::before`.

**Uncommitted right now** (ask the user before committing):
- `CLAUDE.md`: new Commands/Architecture/Game data/Gotchas sections.
- Ponytail cleanup (staged): deleted 7 vendored Neon skills, `skills-lock.json`, `scratch-marble-stone-gen.js`, `neon.ts`, old `README.md` (recreated as a 3-line stub, untracked); removed `@neon/env` and `@neon/config` from `package.json`/lockfile; dropped `export` on 5 internal-only symbols. Lint/build passed.
- `src/app/globals.css`: Party Planner "Screenshot" modal now uses dark text (`#1b1510`, bold, no shadow) for names/classes so they read on the pastel group cards. Verified computed style only; the test board had no assigned players.
- `HANDOFF.md` is this file (untracked).

## Known issues / pending
- Lint: 2 `react/no-unescaped-entities` errors at `src/components/InheritanceTab.js:15` (pre-existing).
- Deferred on purpose: refreshing old items' drop rates (do it next patch when the new Colossus boss ships; the skill says how), and checking for stat changes on older gear.
- Possible further speed-up: the 3.4 MB `loot-data.json` bundle (about half is duplicated trait/resonance lists per item) and the blur on `.custom-select__list`.
- The local app needs a Discord login; the dev server config is `web-dev` in `.claude/launch.json`.
